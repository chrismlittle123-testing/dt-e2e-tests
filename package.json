{
  "name": "dt-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for drift-toolkit",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "setup": "node scripts/setup.js",
    "cleanup": "node scripts/cleanup.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "drift-toolkit": "^3.4.3",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "check-my-toolkit": "^1.9.2"
  }
}
